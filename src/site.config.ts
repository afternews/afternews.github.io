/* ============================================================
 *  ⚙️  블로그 설정 파일 — 여기만 고치면 됩니다
 *  이 파일 하나만 수정하면 사이트 전체(제목, 소개, 광고, 메뉴)가 바뀝니다.
 * ============================================================ */

export const SITE = {
  /** 사이트 제목 (브라우저 탭, 검색 결과에 표시) */
  title: '다시 읽기',

  /**
   * 한 줄 소개 (검색 결과 설명문으로 쓰입니다)
   * ⚠️ 이 문장은 '무엇을 쓰지 않는지'까지 밝히는 역할을 합니다. 함부로 지우지 마세요.
   */
  description:
    '공개된 자료를 놓고 사건이 아니라 구조를 봅니다. 뉴스가 알려 준 다음에 남는 질문들을 통계·판결문·보고서로 다시 따라가 정리합니다.',

  /** 헤더 아래 붙는 짧은 문구 */
  tagline: '뉴스가 지나간 자리에 남는 질문들',

  /** 글쓴이 이름 (필명을 쓰실 거면 필명을 적으세요) */
  author: '사색가 J',

  /** 연락용 이메일 (문의 페이지 / 애드센스 심사에 필요) */
  email: 'kyocap29@gmail.com',

  /**
   * 배포 후의 실제 주소. 끝에 / 를 붙이지 마세요.
   *
   *   GitHub Pages 를 쓰는 경우 → 'https://내아이디.github.io'
   *   Vercel 을 쓰는 경우       → 'https://내프로젝트.vercel.app'
   *   내 도메인을 산 경우       → 'https://내도메인.com'
   *
   * ⚠️ sitemap·RSS·공유 미리보기가 전부 이 값을 씁니다.
   *    배포한 뒤 반드시 실제 주소로 바꾸고 다시 올리세요.
   */
  url: 'https://afternews.github.io',

  locale: 'ko-KR',

  /* ---------------------------------------------------------
   *  🛡️ 고지 설정 — 직장과의 관계를 정리하는 부분입니다
   *  자세한 배경은 「리스크관리.md」를 읽어 보세요.
   * --------------------------------------------------------- */
  disclosure: {
    /**
     * 소속 매체명을 사이트 어디에도 쓰지 않습니다.
     * true 로 두는 것을 강력히 권합니다. (회사 승인을 받았다면 false 로 바꿔도 됩니다)
     */
    hideEmployer: true,

    /**
     * 모든 페이지 하단과 글 끝에 붙는 면책 문구.
     * 나중에 회사가 문제 삼을 때 "개인 자격임을 계속 밝혀 왔다"는 근거가 됩니다.
     */
    disclaimer:
      '이 블로그의 글은 공개된 자료만을 근거로 씁니다. 개인 자격으로 운영하며 어떤 조직의 입장과도 무관하고, 비공개 정보나 취재원에 관한 정보는 다루지 않습니다.',

    /** 상세한 운영 원칙 페이지를 노출할지 (권장: true) */
    showPrinciplesPage: true,
  },

  /** 상단 메뉴 */
  nav: [
    { label: '글', href: '/posts' },
    { label: '검색', href: '/search' },
    { label: '소개', href: '/about' },
    { label: '원칙', href: '/principles' },
  ],

  /** 푸터 링크 (빈 문자열로 두면 표시되지 않습니다) */
  social: {
    email: 'kyocap29@gmail.com',
    x: '',
    instagram: '',
    threads: '',
  },

  /* ---------------------------------------------------------
   *  💰 구글 애드센스
   *  승인 전에는 client를 '' 로 비워 두세요.
   *  승인/게시자 ID를 받으면 'ca-pub-0000000000000000' 형태로 채우면
   *  광고 스크립트와 ads.txt가 자동으로 켜집니다.
   * --------------------------------------------------------- */
  adsense: {
    client: '', // 예: 'ca-pub-1234567890123456'

    /** true면 구글이 알아서 광고를 배치(자동 광고). 처음엔 true 권장 */
    autoAds: true,

    /**
     * 수동 광고 슬롯 ID (애드센스 > 광고 > 광고 단위 기준 에서 발급).
     * 비워 두면 해당 위치의 광고는 표시되지 않습니다.
     *
     * 만드는 법과 각 자리에 어떤 광고 유형을 골라야 하는지는
     * 「수익화-가이드.md」 2장에 그림 순서대로 적어 두었습니다.
     */
    slots: {
      articleTop: '',    // 본문 시작 직전   → '디스플레이 광고'
      inArticle: '',     // 본문 중간(자동)  → '콘텐츠 내 자동 광고(인아티클)'
      articleBottom: '', // 본문 끝          → '디스플레이 광고'
      sidebar: '',       // 데스크톱 오른쪽 고정 → '디스플레이 광고(세로형)'
      listInfeed: '',    // 글 목록 중간     → '인피드 광고'
    },

    /**
     * 본문 중간 광고를 몇 문단마다, 최대 몇 개 넣을지는
     * src/components/InlineAds.astro 맨 위 숫자로 조절합니다.
     * 기본값(4문단마다·최대 3개)이 수익과 읽는 경험의 균형점입니다.
     */
  },

  /** 구글 애널리틱스 4 측정 ID (예: 'G-XXXXXXXXXX'). 없으면 비워 두세요 */
  ga4: '',

  /** 구글 서치콘솔 HTML 태그 인증값 (content="..." 안의 값만). 없으면 비워 두세요 */
  googleSiteVerification: '',

  /** 네이버 서치어드바이저 인증값. 없으면 비워 두세요 */
  naverSiteVerification: '',
} as const;

export type SiteConfig = typeof SITE;
